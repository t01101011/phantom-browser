# Phantom Browser — environment variables
# Copy to .env and fill in real proxy credentials. NEVER commit the real .env.
#
# Format: each proxy is 4 vars (HOST/PORT/USER/PASS). Match the order in
# presets.py and the DB proxy_usage_count check.

# Proxy 1 — residential (Mobite Network LLC, exit Kalispell US)
PROXY_1_HOST=
PROXY_1_PORT=
PROXY_1_USER=
PROXY_1_PASS=

# Proxy 2
PROXY_2_HOST=
PROXY_2_PORT=
PROXY_2_USER=
PROXY_2_PASS=
